- name : demo on greather number
  hosts : local
  connection : local
  vars :
    number of sessions : 4
  tasks :
  - name : check number of sessions 
    ansible.builtin.debug :
     msg : " number of sessions are {{number of sessions}}"
  when : number of sessions > 33